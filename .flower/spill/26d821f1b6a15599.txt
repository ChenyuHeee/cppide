# 审计修复报告(修 `.flower/artifacts/验收审计.md` 的 4 个新问题)

> 日期:2026-09-07 · 环境:Debian aarch64,g++ 14.2.0,ncursesw 6.5,libcurl(gnutls),Python 3.13
> 凭据纪律:**全程未使用、未猜测、未尝试获取任何真实 API key**。所有 AI 路径一律用
> 假 key(`FAKEKEY_*`)+ 本机 `127.0.0.1` 的假 SSE 端点(`audit-fake-ai-server.py`),未联网。
> 目标平台 macOS,只用 POSIX;本轮全部实测在 Linux 上完成(与审计报告 §6 同一未验证面)。

---

## 0. 一句话结论

四项全部落地并实测通过;回归五项 + 审计留下的 6 个 `audit-*` 脚本 + ASan/UBSan 端到端全过。
`src/*.h` 只在 **private 区加了 1 个成员 + 1 个常量**,没有改动任何已有公开签名。

---

## 1. P1 —— 写代码模式空转无限重发请求(花钱)

### 修法

=====185-260
## 5. 回归结果(全部真跑)

| 项 | 结果 |
|---|---|
| `make check-headers` | OK(**15 个头文件**) |
| `make clean && make -j4` | exit=0,`warning: ` **0 条**,`error: ` 0 条 |
| `make tests` | exit=0,15 个测试全过(test_ai 15 cases / test_config 417 断言 / test_ui 833467 断言 …) |
| `bash .flower/scripts/wave5-verify.sh` | **失败 0 条**(exit=0)。日志里出现 1 行 `FAIL expectlast "退出码"`,来自脚本自己 `|| true` 容忍的 B3b 洪流中途用例(时序敏感、非本轮改动引入,汇总未计入) |
| `bash .flower/scripts/wave5-finalfix-verify.sh` | **失败 0 条**(exit=0) |
| `audit-ai-modes.sh`(验收 6/7,本地假端点) | 17/17 PASS(ghost 显示 + 属性与正文不同 + Tab 接受 + Esc 丢弃 + 延时可配;练习模式 sha256 逐字节未变、Tab 不引入 AI 内容) |
| `audit-config-key.sh`(验收 8) | 15/15 PASS |
| `audit-net-failures.sh`(验收 9) | 16/16 PASS |
| `audit-open-edit-save.sh`(验收 2) | 5/5 PASS,五类 token 仍是 5 种不同 SGR |
| `audit-build-run-mode.sh`(验收 3/4/5) | 15/15 PASS |
| `audit-readme-keys.sh`(验收 10) | 28/28 PASS(README 改动后重跑) |
| `audit-idle-cost.sh`(P1 复现脚本) | 1 次 → PASS |
| `audit-asan-smoke.sh`(验收 12,`./cppide-debug`) | 15/15 PASS,**ASan/UBSan/LSan 全程零报告**(含 AI 三态 + ghost 接受/丢弃 + 练习模式) |
| `fix-p1/p3/p4` 在 ASan 版下重跑 | 全 PASS,零 sanitizer 报告 |

---

## 6. 改动清单

产品代码:

| 文件 | 改了什么 |
|---|---|
| `src/ai.h` | private 区 +`kNoAutoGen` +`auto_done_gen_`;`maybeAutoTrigger` 的条件注释补一条 |
| `src/ai.cpp` | `enabled()` 加 model 非空;构造用 `enabled()`;`maybeAutoTrigger`/`setMode`/`cancelInFlight`/`askNow` 各一处额度操作;新增 `kZhNoModel` 文案 |
| `src/config.h` | `model` 默认值改空串 + 降级契约注释;`ghost_min_interval_ms` 注释 |
| `src/config.cpp` | model 为空的中文 warning;`sampleJson()` 的 model 留空 + 注释重写(model / ghost) |
| `src/app.cpp` | `aiUnconfiguredMsg()` 区分缺 key / 缺 model |
| `src/main.cpp` | `--doctor` 的 model 行与结论行区分缺 key / 缺 model |
| `src/ui.cpp` | `P_Ghost` pair 的蓝色占位改成 `-1` + 两处注释(8 色/单色走 `A_DIM`) |
| `Makefile` | debug 改为独立产物 `cppide-debug` + `src/*.dbg.o`;`clean` 与 `-include` 跟上;注释 |
| `README.md` | `make debug`/`make clean` 行、`model` 字段行、`ghost_min_interval_ms` 行、§3 标题与正文、§5.2 新增"同一次停顿只请求一次"、§9 错误表新增 model 一行、§10 第 17 条 |
| `config.sample.json` | 用 `./cppide --print-config` 重新生成(model 留空 + 两条注释更新) |

脚本(`.flower/scripts/`):

| 文件 | 说明 |
|---|---|
| `fix-p1-idle-verify.sh` | **新** P1 验证:30 秒空转 == 1 次;+1 字符再等 == 2 次;ghost 仍显示。支持 `CPPIDE_BIN` |
| `fix-p2-make-verify.sh` | **新** P2 验证:release/debug 并行各 3 轮、互不删产物、mtime、sanitizer 符号、增量、check-headers/tests |
| `fix-p3-model-verify.sh` | **新** P3 验证:三种配置组合 + 静态 grep 断言。支持 `CPPIDE_BIN` |
| `fix-p4-ghost-attr.sh` | **新** P4 验证:`PTY_TERM=xterm` / `xterm-256color` 各渲染一次,从 `.raw` 断言属性 |
| `fix-sgr-at-marker.py` | **新** 从原始 ANSI 流里抓"某段文字写出时生效中的 SGR" |
| `wave5-pty-drive.cpp` | 加 `PTY_TERM` 支持(默认不变);用法注释里写明 `raw_out` 的坑 |
| `audit-asan-smoke.sh` | 默认用 `./cppide-debug`(`CPPIDE_BIN` 可覆盖),否则会永远跳过 |
| `audit-config-key.sh` | 把"model 默认占位值"的说明 echo 换成真的 grep 断言 |

---

## 7. 遗留风险 / 未验证

1. **macOS 未验证**:与审计报告 §6 同一风险面。本轮新增的 Makefile `src/%.dbg.o` 模式规则用的是
   GNU make 语法,macOS 自带 `make` 就是 GNU make 3.81,`filter-out`/静态模式都支持;
   但 3.81 上 `debug -j` 我没跑过。
2. **`Config::aiEnabled()` 与 `AiService::enabled()` 语义不同**(前者只看 key)。
   已在 `config.h` 注释里写明。将来若有人拿 `aiEnabled()` 去判"AI 能不能用",会得到偏乐观的答案 ——
   当前全工程只有 `main.cpp` 的 `--doctor` 文案用到它,那两处已单独处理。
3. **P1 的额度语义是"每个 generation 一次"**。`gen_` 也被 `askNow`/`cancelInFlight`/`setMode` 推进,
   我在这三处都主动"关额度";如果以后有人再加一处 `gen_++` 而忘了决定额度归属,
   默认行为会变成"开额度"(可能多发一次请求)。`tests/test_ai.cpp::case_trigger_and_rate_limit`
   能挡住最明显的回归,但它不覆盖"新加的取消点"。
4. **`wave5-verify.sh` 里 B3b 那条 `FAIL`** 是脚本自己 `|| true` 容忍的时序敏感用例,
   与本轮改动无关(它测的是 1.5MB 输出洪流中途的面板刷新),但它每次都会在日志里出现,容易被误读。
5. `fix-p1-idle-verify.sh` 单次要跑 ~90 秒(两轮 30 秒空转),不适合放进快速回归。