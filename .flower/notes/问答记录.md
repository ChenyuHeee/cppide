# 需求问答记录

> 自动追加。这一份不进上下文,只作留档。

- **Q**(q1) 这个编辑器你想要哪种形态？（决定整个技术栈，最关键）
  - 选项:桌面 GUI，Windows 上用（Electron/Tauri 之类，前端技术做界面） / 桌面 GUI，Linux 上用 / 跨平台桌面 GUI（Windows + Linux 都要跑） / 终端里的 TUI 编辑器（类似 vim 风格，轻量） / 浏览器里的网页编辑器（本地起个服务，浏览器打开） / 你替我选一个最合适的
  - **A**[answered] 4 我是mac
- **Q**(q2) 终端编辑器这块：是从零写一个独立的编辑器，还是做成 neovim 插件（复用现成的编辑能力）？
  - 选项:从零写一个独立的 TUI 编辑器（自己的可执行文件，不依赖 vim/neovim） / 做成 neovim 插件（编辑功能用现成的，我只要 AI + 编译那块） / 都行，你选个能快点用上的
  - **A**[answered] 1 而且只需要C语言和C++
- **Q**(q3) AI 补全怎么触发？（你说是给 ICPC 练习用的，自动补全整段代码可能反而妨碍训练）
  - 选项:像 Copilot 一样：停顿就自动出灰色建议，Tab 接受（最像 VSCode，但练题时容易直接抄答案） / 只在我按快捷键时才补全（不打断思路，不乱花钱） / 两个都要，用一个开关切：“练习模式”只给思路不给代码，“写代码模式”才自动补全
  - **A**[answered] 两个都要，用一个开关切：“练习模式”只给思路不给代码，“写代码模式”才自动补全
- **Q**(q4) DeepSeek 这边：模型 ID 用哪个？（我这边不确定 "deepseek-v4" 是不是官方 API 里的有效模型名，写死了可能直接调不通）
  - 选项:我有 key，模型 ID 就写 deepseek-chat（官方通用入口，自动指向最新版） / 我有 key，但具体模型 ID 我不确定，做成配置项我自己填 / 我还没 key，先把代码写好，模型名和 key 都走配置文件
  - **A**[answered] 我还没 key，先把代码写好，模型名和 key 都走配置文件
